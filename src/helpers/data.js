/**
  @param {string} dataStr
  @param {string} transformType - how you want your data to be transformed
  @returns {String} transofrmed data
*/
export default transformData({dataStr, transformType}) => {

  return dataStr
}
